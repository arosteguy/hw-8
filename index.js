const fs = require("fs");
const axios = require("axios");
const inquirer = require("inquirer");

inquirer
   prompt.rawlist({

    })



const questions = [
  
];

function writeToFile(fileName, data) {
 
}

function init() {

init();
